# Adding an episode
Copy the demo directory, change YAML metadata/content, reuse environments and characters, validate, synthesize voice, compile and preview. Add library components only when the desired semantic behavior cannot be expressed by an existing one. Never hand-edit generated timeline or subtitles.
