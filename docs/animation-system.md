# Animation system
Animations use `useCurrentFrame()` and deterministic trigonometric/spring/interpolation functions. Semantic actions are compiled from each shot; dialogue-relative talk state comes from the measured dialogue interval. Add reusable actions to the schema, types and rig resolver together.
