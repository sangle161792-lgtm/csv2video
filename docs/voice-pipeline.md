# Voice pipeline
`TTSProvider` isolates synthesis. Edge uses the natural Vietnamese female `vi-VN-HoaiMyNeural`. Replace audio through a provider, then rerun timeline and render; animation and subtitles follow measured duration. The first CLI supports Edge and gives an explicit error for providers not installed. Edge TTS requires network only during synthesis.
