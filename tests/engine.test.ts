import {describe,it,expect} from 'vitest';import {EpisodeSchema} from '../src/engine/schema';import {secondsToFrames} from '../src/engine/time';import {heuristicLipSync} from '../src/engine/lipsync';import {loadEpisode} from '../src/engine/load';import {compileTimeline} from '../src/engine/timeline';
describe('engine',()=>{it('validates demo YAML',()=>expect(()=>loadEpisode('episodes/001-ech-ngoi-day-gieng')).not.toThrow());it('rejects unknown animation',()=>expect(EpisodeSchema.safeParse({episode:{id:'x',title:'x',fps:30,width:1,height:1},shots:[{id:'s',duration:1,environment:'x',camera:{shot:'wide'},actions:[{type:'bragg'}]}]}).success).toBe(false));it('converts seconds deterministically',()=>expect(secondsToFrames(1.5,30)).toBe(45));it('resolves mouth states',()=>expect(new Set(heuristicLipSync(1).map(x=>x.state)).size).toBe(4));it('compiles ordered dialogue duration',async()=>{const t=await compileTimeline(loadEpisode('episodes/001-ech-ngoi-day-gieng'),'episodes/001-ech-ngoi-day-gieng');expect(t.duration).toBe(64);expect(t.dialogues['frog-001'].end).toBeGreaterThan(t.dialogues['frog-001'].start)});});
